name: Setup Redmine
description: Setup Redmine
inputs:
  redmine-version:
    description: version of Redmine
    default: 'trunk'
runs:
  using: docker
  image: Dockerfile
  args:
    - ${{ inputs.redmine-version }}
